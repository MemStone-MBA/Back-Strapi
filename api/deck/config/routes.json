{
  "routes": [
    {
      "method": "GET",
      "path": "/deck",
      "handler": "deck.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/deck/count",
      "handler": "deck.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/deck/:id",
      "handler": "deck.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/deck",
      "handler": "deck.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/deck/:id",
      "handler": "deck.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/deck/:id",
      "handler": "deck.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
